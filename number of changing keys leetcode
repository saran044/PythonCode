class Solution:
    def countKeyChanges(self, s: str) -> int:
        c=0
        d=s.lower()
        for i in range(len(d)-1):
            if d[i]!=d[i+1]:
                c=c+1
        return c
        
