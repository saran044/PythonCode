  l=[]
        ml=[]
        for i in range(numRows):
            for j in range(i+1):
                l.append(1)
            ml.append(l)
            l=[]
        for i in range(len(ml)):
            if len(ml[i])>2:
                for j in range(1,len(ml[i])-1):
                    a=ml[i-1][j-1]+ml[i-1][j]
                    ml[i][j]=a
        return ml            
